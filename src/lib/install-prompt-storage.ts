// Single source of truth for the install-prompt sync-success flag.
// Shared so the sync engine and InstallPrompt component never drift.
export const SYNC_SUCCESS_KEY = "install-prompt:syncSucceeded";
